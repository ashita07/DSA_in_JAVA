fun main()
{
    var x =5f
    val i =3

    println ("${x/i}")
val myNumber = 100
    if(myNumber != 150)
        println("less than 150 ")
   else if(myNumber >90)
        println("Greater than 90")
else
 println("all the conditions failed")
val isPlaying = false
    val score =100
    if(isPlaying || score==80)
        println("next level opened")
    else
        println("still at same level")

    val num1=-7
    val num2=7
    val text :String = if(num1>0 || num2<0) "d" else "g"
    println(text)
}